# CI по контракту `quality-runtime/v1`.
#
# СТАТУС: регресс-гейт БЛОКИРУЕТ (2026-09-02). `continue-on-error` с него снят:
# новое падение сверх baseline красит сборку. Это и закрывает дыру «новая
# backend-регрессия оставляет CI зелёным».
#
# Полосы при этом наблюдают падения тестов, а не блокируют, и это не
# послабление, а разделение ролей: полоса знает только свой прогон, сравнение с
# известным долгом делает гейт. Пока baseline непуст, блокировка по полосе даёт
# КРАСНЫЙ CI НА КАЖДОМ ПРОГОНЕ — измерено, полоса `unit` падает ровно на двух
# записях baseline. Непригодный прогон (коды 2/3/4/5) полоса красит всегда.
#
# Остаётся одно послабление с названным условием снятия (см. конец файла):
# профиль полос — `--ci`, а не `--enforce`. §3.3 и §6 правило 4 привязывают
# обязательность norm corpus именно к enforce, а источник корпуса владельцем не
# назван.
#
# Отдельного исключения по полосе `network` БОЛЬШЕ НЕТ. Оно стояло, пока полоса
# не была проверена целиком под нагрузкой профиля раннера; 2026-09-02 такой
# прогон выполнен: 4 ядра (taskset), конкурирующая нагрузка, LA на ядро от 3.4
# до 6 — вчетверо выше верхнего порога s_la, — пять полных проходов, ноль
# падений, ноль осиротевших процессов, 437–504 с при бюджете §7 в 1200 с.
# Доказательство — receipts/evidence/0.0.03-network-runner-profile-manifest.json.
#
# Job `chaos` остаётся observe-only по назначению: он убивает процессы и
# существует, чтобы ВИДЕТЬ недетерминизм, а не блокировать merge.
#
# Что даёт этот workflow сверх прежнего:
#   - установка одной воспроизводимой командой с lock-профилем `qr-v1`;
#   - capability probe ДО тестов: непригодное окружение отказывает сразу и
#     называет причину, а не даёт «странные» падения через десять минут;
#   - пять lane-job'ов по §5.1, каждый со своими бюджетами §7 и своим JUnit;
#   - JSON receipt §8 на каждый прогон, все отчёты выгружаются артефактами;
#   - provisioning norm corpus отдельным проверяемым шагом.
#
# Контракт окружения — docs/architecture/QUALITY_RUNTIME_CONTRACT_V1.md.
# Эксплуатация — docs/ops/TEST_HARNESS_RUNBOOK.md.
#
# Владение файлом: `W0-INT-01` по ограниченному pre-G1 исключению
# (ADR-0006 §4.2). После G1 файл возвращается к `W1-OPS-02`.

name: tests

on:
  push:
  pull_request:

# §3.1: значения профиля задаются один раз и наследуются всеми job'ами.
# Раньше их приходилось повторять в каждом шаге, и они успели разойтись.
env:
  PYTHON_VERSION: "3.12.3"
  NODE_VERSION: "22.23.1"
  AUDIT_DISABLE_DOTENV: "1"
  PAID_API_ENABLED: "false"
  PORTAL_AUTH_ENABLED: "false"
  TZ: UTC
  LC_ALL: "C.UTF-8"
  # §8 требует source_commit в каждом receipt. В Actions provenance даёт
  # сам checkout, но объявляем явно: в дереве без `.git` (например
  # развёрнутом из архива) probe иначе откажется выпускать расписку.
  QR_SOURCE_COMMIT: ${{ github.sha }}
  PYTHONHASHSEED: "0"

jobs:
  # ───────────────────────────────────────────────────────────────────────────
  # Пять lane-job'ов по §5.1. Каждый со своим бюджетом, своим JUnit и своим
  # receipt. Разделены не ради красоты: §7 задаёт РАЗНЫЕ бюджеты на lane, а
  # общий прогон свёл бы их к худшему.
  # ───────────────────────────────────────────────────────────────────────────
  lanes:
    name: "lane ${{ matrix.lane }}"
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false        # один упавший lane не скрывает состояние прочих
      matrix:
        lane: [unit, contract, integration, network]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      # §3.1 — один профиль на весь прогон. check_profile_pins в --enforce
      # требует пины Node/npm ДАЖЕ в python-полосах: контракт описывает машину
      # целиком, а не только её питоновскую половину.
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      # Установка ОДНОЙ командой с lock-профилем. Прежняя схема «шесть -r
      # подряд плюс ad-hoc pip install pytest» не воспроизводила frozen
      # receipt §4.1: часть версий приезжала из манифеста стороннего VPS.
      # Без `|| true`: молча пропущенная зависимость даёт падения,
      # неотличимые от регрессий.
      - name: Установить зависимости профиля qr-v1
        run: |
          python -m pip install --upgrade pip
          pip install -c constraints-qr-v1.txt -r requirements-dev.txt

      # Здесь только отпечаток в лог и связность набора. Сверку с §4.1
      # делает probe внутри `ci_test_lane.py` (PIP_FREEZE_DRIFT); она не
      # дублируется, чтобы рецепт оставался в одном месте.
      - name: Отпечаток набора зависимостей и `pip check`
        run: |
          echo "pip freeze sha256: $(python -m pip freeze | sha256sum | cut -d' ' -f1)"
          python -m pip check

      # Источник корпуса задаётся ТОЛЬКО конфигурацией репозитория, адрес в
      # workflow не зашит: зашитый источник — неотзываемое доверие. Форма
      # приёмки и смысл каждой переменной — docs/ops/NORM_ARTIFACT_SOURCE.md.
      #
      # Пока владелец источник не назвал, ни одна переменная не задана, и шаг
      # ведёт себя ровно как прежде: OPTIONAL_NORM_CORPUS_ABSENT и exit 0.
      # Как только источник задан, `--enforce-if-configured` делает шаг
      # fail-closed: недоступность, несовпадение SHA-256 или отсутствие
      # эталона — setup failure, а не warning и не skip.
      - name: Provisioning norm corpus
        env:
          QR_NORM_ARTIFACT_SOURCE_ID: ${{ vars.QR_NORM_ARTIFACT_SOURCE_ID }}
          QR_NORM_ARTIFACT_URL: ${{ vars.QR_NORM_ARTIFACT_URL }}
          QR_NORM_ARTIFACT_VERSION: ${{ vars.QR_NORM_ARTIFACT_VERSION }}
          QR_NORM_ARTIFACT_SHA256: ${{ vars.QR_NORM_ARTIFACT_SHA256 }}
          # Схема Authorization — не credential, поэтому vars, а не secrets.
          # Пока переменная не заведена, приезжает пустая строка и скрипт
          # берёт умолчание Bearer. Без этой строки поле 12 формы приёмки
          # было недостижимо: небезерная авторизация не заработала бы.
          QR_NORM_ARTIFACT_AUTH_SCHEME: ${{ vars.QR_NORM_ARTIFACT_AUTH_SCHEME }}
          # Секрет виден ТОЛЬКО этому шагу: в env job'а он попал бы в окружение
          # pytest, а §3.1 требует прогон без provider credentials.
          QR_NORM_ARTIFACT_TOKEN: ${{ secrets.QR_NORM_ARTIFACT_TOKEN }}
        run: >-
          python scripts/ci_provision_norms.py
          --acquire --build-index --enforce-if-configured --json

      # §3.1: uid != 0. Под root режимы 0o555 не действуют, и проверки
      # неудаляемости запечатанного дерева проходят вхолостую.
      - name: Проверить, что прогон не под root
        run: test "$(id -u)" -ne 0 || { echo "::error::раннер под root — нарушение §3.1"; exit 1; }

      # Раннер сам выполняет probe ДО pytest (§6, правило 1): отсутствие
      # обязательной capability — setup failure, а не skip.
      #
      # ПОЧЕМУ ПАДЕНИЕ ТЕСТА ЗДЕСЬ НЕ КРАСИТ JOB, а красит его гейт.
      # Полоса знает только собственный прогон. Она НЕ знает baseline и не умеет
      # отличить новую регрессию от известного долга — это по построению делает
      # job `regression-gate`, который сравнивает прогон со списком
      # scripts/ci_known_failures.txt. Пока baseline непуст, блокировка по
      # полосе означает КРАСНЫЙ CI НА КАЖДОМ ПРОГОНЕ: измерено в чистой комнате
      # 2026-09-02 — полоса `unit` даёт exit 1 ровно на двух оставшихся записях
      # baseline и ни на чём другом. Постоянно красный CI хуже маскировки: его
      # перестают читать, и настоящая регрессия тонет в привычном красном.
      #
      # Дыру, ради которой это правилось, закрывает гейт: с него снят
      # `continue-on-error`, и НОВОЕ падение — то есть регрессия — красит сборку.
      # Покрытие эквивалентно: гейт гоняет выборку по умолчанию (`-m "not chaos"`),
      # в неё входят те же узлы, что и в полосы unit/contract/integration/network.
      #
      # Условие, при котором блокировка переедет на полосы: baseline опустеет
      # (две оставшиеся записи принадлежат WEB и ENG, см. шапку
      # ci_known_failures.txt) либо полоса научится сравнивать прогон с
      # baseline сама. До тех пор ответственность за «красное» лежит на гейте, и
      # это записано, чтобы никто не принял наблюдение за отсутствие проверки.
      # Отдельного исключения по `network` больше нет: полоса проверена целиком
      # под нагрузкой профиля раннера 2026-09-02, пять проходов без падений.
      #
      # Блокирующими остаются и шаги ВЫШЕ: установка по constraints, pip check,
      # provisioning, проверка uid != 0. Отказ инфраструктуры — не наблюдение.
      # НЕ `continue-on-error` на весь шаг: он глушил и код 2 (probe не пропустил
      # lane), и код 4 (испорченный JUnit), и код 5 (пустая выборка). Непригодный
      # прогон обязан быть красным, иначе «зелёный CI» начинает означать «мы
      # ничего не проверили».
      - name: "Lane ${{ matrix.lane }}"
        env:
          AUDIT_APP_DATA_DIR: ${{ runner.temp }}/run-root/app_data
          AUDIT_PROJECTS_DIR: ${{ runner.temp }}/run-root/projects
          AUDIT_OBJECTS_FILE: ${{ runner.temp }}/run-root/objects.json
          AUDIT_ACTION_LOG_DIR: ${{ runner.temp }}/run-root/action_log
          AUDITMANAGER_DEPLOY_LOCK_DIR: ${{ runner.temp }}/run-root/locks
        run: |
          set +e
          python scripts/ci_test_lane.py --lane ${{ matrix.lane }} --use-lane-marker --ci
          rc=$?
          set -e
          # 0 — чисто. 1 — упали тесты: НАБЛЮДАЕТСЯ, потому что полоса не знает
          # baseline; блокирует job `regression-gate`, который его знает.
          # 2 setup failure, 3 бюджет §7, 4 негодный отчёт, 5 пустая выборка —
          # прогон, из которого нельзя делать выводов; пробрасывается всегда.
          if [ "$rc" -eq 0 ]; then exit 0; fi
          if [ "$rc" -eq 1 ]; then
            echo "::warning::полоса ${{ matrix.lane }} упала (code 1). Блокирует регресс-гейт: он сравнивает прогон с baseline, а полоса этого не умеет"
            exit 0
          fi
          echo "::error::ci_test_lane вернул $rc — прогон непригоден (не падение теста)"
          exit "$rc"

      # Отчёты выгружаются ВСЕГДА: при падении они нужнее всего. §8 требует
      # свежий JUnit и JSON receipt на каждый прогон.
      - name: Выгрузить отчёты lane
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lane-${{ matrix.lane }}-reports
          path: .ci/reports/
          if-no-files-found: warn

  # ───────────────────────────────────────────────────────────────────────────
  # Legacy aggregate против baseline. Остаётся до тех пор, пока lane-receipt'ы
  # не станут единственным источником вердикта: он сравнивает с известным
  # долгом, а lanes этого не делают.
  # ───────────────────────────────────────────────────────────────────────────
  regression-gate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      # §3.1 — один профиль на весь прогон. check_profile_pins в --enforce
      # требует пины Node/npm ДАЖЕ в python-полосах: контракт описывает машину
      # целиком, а не только её питоновскую половину.
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Установить зависимости профиля qr-v1
        run: |
          python -m pip install --upgrade pip
          pip install -c constraints-qr-v1.txt -r requirements-dev.txt

      - name: Связность набора зависимостей (`pip check`)
        run: python -m pip check

      # Решение C-2: нормативный контур проверяется, а не пропускается.
      # Корпус лежит вне git и обязан приезжать с checksum.
      #
      # ОЖИДАЕТ ВЛАДЕЛЬЦА: источник не назван. Ветвление идёт по КОНФИГУРАЦИИ,
      # а не по наличию каталога. Прежняя ветка `if [ -d norms/vault ]` была
      # маскировкой сразу дважды: во-первых, отсутствие обязательного входа она
      # понижала до `::warning::`, то есть до зелёного job'а; во-вторых, любой
      # каталог `norms/vault`, взявшийся на раннере откуда угодно, она молча
      # принимала за корпус. Теперь оба состояния названы:
      #   источник не задан  → OPTIONAL_NORM_CORPUS_ABSENT, exit 0 (как прежде);
      #   источник задан     → fail-closed, любой сбой получения = setup failure.
      #
      # norms/tools/README.md прямо запрещает собирать корпус из norms_db.json,
      # поэтому внутри репозитория его получить нельзя: это внешний вход.
      # Что обязан предоставить владелец — docs/ops/NORM_ARTIFACT_SOURCE.md.
      - name: Provisioning norm corpus
        env:
          QR_NORM_ARTIFACT_SOURCE_ID: ${{ vars.QR_NORM_ARTIFACT_SOURCE_ID }}
          QR_NORM_ARTIFACT_URL: ${{ vars.QR_NORM_ARTIFACT_URL }}
          QR_NORM_ARTIFACT_VERSION: ${{ vars.QR_NORM_ARTIFACT_VERSION }}
          QR_NORM_ARTIFACT_SHA256: ${{ vars.QR_NORM_ARTIFACT_SHA256 }}
          # Схема Authorization — не credential, поэтому vars, а не secrets.
          # Пока переменная не заведена, приезжает пустая строка и скрипт
          # берёт умолчание Bearer. Без этой строки поле 12 формы приёмки
          # было недостижимо: небезерная авторизация не заработала бы.
          QR_NORM_ARTIFACT_AUTH_SCHEME: ${{ vars.QR_NORM_ARTIFACT_AUTH_SCHEME }}
          QR_NORM_ARTIFACT_TOKEN: ${{ secrets.QR_NORM_ARTIFACT_TOKEN }}
        run: >-
          python scripts/ci_provision_norms.py
          --acquire --build-index --enforce-if-configured --json

      - name: Проверить, что прогон не под root
        run: test "$(id -u)" -ne 0 || { echo "::error::раннер под root — нарушение §3.1"; exit 1; }

      - name: Capability preflight
        run: python scripts/ci_runtime_probe.py --profile unit

      # Гейт ограничен по времени (находка OPS03-F10): на машине с зависающим
      # тестом он отказывает с диагнозом, а не висит до таймаута job'а.
      # `continue-on-error` СНЯТ 2026-09-02. Он стоял, пока 33 из 35 записей
      # baseline держались на нерабочих тестах норм: гейт был бы красным на
      # каждом прогоне. Тесты починены, записи сняты, гейт на чистом дереве даёт
      # «новых 0». Дальше continue-on-error означал бы ровно одно: новая
      # backend-регрессия оставляет CI зелёным.
      - name: Регресс-гейт (падает только на НОВЫХ падениях против baseline)
        env:
          CI_GATE_WALL_BUDGET_SEC: "1800"
          AUDIT_PROJECTS_DIR: ${{ runner.temp }}/run-root/projects
          AUDIT_OBJECTS_FILE: ${{ runner.temp }}/run-root/objects.json
          AUDIT_ACTION_LOG_DIR: ${{ runner.temp }}/run-root/action_log
          AUDITMANAGER_DEPLOY_LOCK_DIR: ${{ runner.temp }}/run-root/locks
        run: python scripts/ci_regression_gate.py

      - name: Выгрузить отчёт гейта
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-gate-report
          path: .ci_last_report.xml
          if-no-files-found: warn

  # ───────────────────────────────────────────────────────────────────────────
  # Frontend gate §10. Node пинится явно: полагаться на PATH раннера нельзя —
  # ровно на этом clean-room репетиция получила node 18 вместо 22 (CR-5).
  # ───────────────────────────────────────────────────────────────────────────
  frontend:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # `npm ci`, а не `npm install`: ci ставит РОВНО lockfile и падает при
      # расхождении с package.json, install молча правит lockfile.
      - name: Установить зависимости
        run: npm ci

      - name: Версии инструментов
        run: |
          echo "node $(node -v), npm $(npm -v)"
          test "$(node -v)" = "v${{ env.NODE_VERSION }}" \
            || { echo "::error::Node не соответствует пину §3.1"; exit 1; }

      - name: Тесты
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Strict typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

  # ───────────────────────────────────────────────────────────────────────────
  # Решение C-5: chaos поднимает реальные процессы, убивает их сигналами и
  # зависит от таймингов раннера. Отдельный observe-only job — его назначение
  # видеть недетерминизм, а не блокировать merge.
  # ───────────────────────────────────────────────────────────────────────────
  chaos:
    runs-on: ubuntu-24.04
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      # §3.1 — один профиль на весь прогон. check_profile_pins в --enforce
      # требует пины Node/npm ДАЖЕ в python-полосах: контракт описывает машину
      # целиком, а не только её питоновскую половину.
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Установить зависимости профиля qr-v1
        run: |
          python -m pip install --upgrade pip
          pip install -c constraints-qr-v1.txt -r requirements-dev.txt

      # Provisioning нужен и здесь. `severity_for` в ci_runtime_probe делает
      # norm_artifact ОБЯЗАТЕЛЬНЫМ во всех пяти профилях при `--enforce`, включая
      # chaos. Без этого шага перевод полос в enforce (шаг порядка включения в
      # конце файла) дал бы chaos setup failure с NORM_ARTIFACT_MISSING, а из-за
      # `continue-on-error: true` на job это выглядело бы как «chaos отработал».
      # На деле полоса, существующая ради наблюдения недетерминизма, просто
      # перестала бы выполняться — и потеря была бы тихой. Шаг добавлен ДО
      # перехода в enforce намеренно: сейчас он ничего не меняет (источник не
      # задан, поведение прежнее), а на шаге перехода уже будет на месте.
      - name: Provisioning norm corpus
        env:
          QR_NORM_ARTIFACT_SOURCE_ID: ${{ vars.QR_NORM_ARTIFACT_SOURCE_ID }}
          QR_NORM_ARTIFACT_URL: ${{ vars.QR_NORM_ARTIFACT_URL }}
          QR_NORM_ARTIFACT_VERSION: ${{ vars.QR_NORM_ARTIFACT_VERSION }}
          QR_NORM_ARTIFACT_SHA256: ${{ vars.QR_NORM_ARTIFACT_SHA256 }}
          QR_NORM_ARTIFACT_AUTH_SCHEME: ${{ vars.QR_NORM_ARTIFACT_AUTH_SCHEME }}
          QR_NORM_ARTIFACT_TOKEN: ${{ secrets.QR_NORM_ARTIFACT_TOKEN }}
        run: >-
          python scripts/ci_provision_norms.py
          --acquire --build-index --enforce-if-configured --json

      - name: Проверить, что прогон не под root
        run: test "$(id -u)" -ne 0 || { echo "::error::раннер под root — нарушение §3.1"; exit 1; }

      - name: Chaos lane
        env:
          AUDIT_APP_DATA_DIR: ${{ runner.temp }}/run-root/app_data
          AUDIT_PROJECTS_DIR: ${{ runner.temp }}/run-root/projects
          AUDIT_OBJECTS_FILE: ${{ runner.temp }}/run-root/objects.json
          AUDIT_ACTION_LOG_DIR: ${{ runner.temp }}/run-root/action_log
          AUDITMANAGER_DEPLOY_LOCK_DIR: ${{ runner.temp }}/run-root/locks
        run: python scripts/ci_test_lane.py --lane chaos --use-lane-marker --ci

      - name: Выгрузить отчёты chaos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lane-chaos-reports
          path: .ci/reports/
          if-no-files-found: warn

# ─── Условия перевода гейта в enforce ────────────────────────────────────────
#
# Выполнено:
#   ✔ установка воспроизводима: один lock-профиль, `pip check` зелёный,
#     `pip freeze` совпадает с frozen receipt §4.1;
#   ✔ прогон не под root, изоляция состояния по §3.2 включая lock-каталог;
#   ✔ capability probe выполняется ДО тестов в режиме --ci (§6 правило 1: в
#     CI-полосе послаблений §6.2 нет, профильные проверки §3 обязательны),
#     и его отказ
#     КРАСИТ job: наблюдается только код 1 (упали тесты), а 2/3/4/5 —
#     непригодный прогон — пробрасываются наружу;
#   ✔ пины §3.1 заданы точно: Python 3.12.3 (не 3.12), Node/npm во ВСЕХ
#     job'ах (check_profile_pins требует их и в python-полосах), TZ, LC_ALL,
#     PYTHONHASHSEED;
#   ✔ пять lanes с бюджетами §7, JUnit и JSON receipt §8, отчёты выгружаются;
#   ✔ гейт ограничен по времени и не может висеть бесконечно;
#   ✔ frontend gate с пиннингом Node и strict typecheck;
#   ✔ у каждой записи baseline есть владелец, задача закрытия и срок.
#
# Закрыто волной 0.0.03 — пять недетерминированных узлов полосы `network`:
#   ◐ три теста настоящих процессов читали телеметрию ЖИВОГО хоста. Политика
#     ёмкости воркера берёт её из ДВУХ источников — psutil (RAM/своп) и stdlib
#     `os` (`getloadavg`/`cpu_count`), — а изолирован был только первый. `s_la`
#     считается как LA5 на ядро и при значении >= 1.0 срезает ёмкость до
#     `configured_max - 1`, при >= 1.5 — до нуля; гистерезис роста удерживает
#     заниженное значение дольше ожидания теста. На занятой машине «два
#     одновременных процесса» не наступали никогда — центр честно не выдавал
#     второе задание. Контрольный опыт: до правки 8 красных прогонов из 8,
#     после — 8 зелёных из 8 при той же нагрузке.
#     Доказательство — receipts/W0-INT-01-network-determinism.json.
#
#   ◐ два теста шлюза синхронизировались на одном сигнале, а утверждали про
#     другой. `test_r_u_scheduler_atomic_offer_and_lost_delivery_recovery` читал
#     `delivered_at` сразу после получения offer клиентом, тогда как планировщик
#     сначала кладёт ответ в outbound queue и фиксирует доставку следующим
#     `await`: окно записи измерено, 0.26–24.2 мс. Порядок в проде правильный и
#     не менялся — запись до enqueue пометила бы доставленным неотправленный
#     offer. Второй узел (`test_aq_av_..._lost_ack_resends_...`) давал четырём
#     сообщениям фиксированные 100 мс на все четыре. Контрольные опыты: 6 из 20
#     красных → 0 из 20, и 19 из 20 → 0 из 20. Независимая проверка интегратора,
#     чередуя редакции на одном стенде: старая 5 красных из 18, новая 0 из 18,
#     все пять падений одной сигнатуры.
#     Доказательство — receipts/W0-INT-01-nb1-determinism.json.
#
#     Production-кода во всех пяти случаях не тронуто ни строки, ни один порог
#     не ослаблен, ни одного skip/xfail не добавлено, выборка полосы не
#     изменилась иначе как ростом от работ волны.
#
# НЕ выполнено — два оставшихся условия:
#   ✘ полоса `network` не проверена под нагрузкой целиком. Пять полных проходов
#     сняты на тихой машине, два — при LA1 ~30–40; при LA1 ~100 полоса не
#     прогонялась ни разу (бюджет §7 — 1200 с, при такой нагрузке проход не
#     укладывается). Последний найденный узел показывался ТОЛЬКО под нагрузкой,
#     поэтому «известных узлов не осталось» — это измеренная граница, а не
#     гарантия. До перевода полосы в блокирующую требуется прогон под нагрузкой
#     на профиле раннера.
#
#   ✘ источник norms/vault не назван владельцем. §6 правило 4 контракта
#     объявляет корпус обязательным для enforce, а norms/tools/README.md
#     запрещает собирать его из norms_db.json — это внешний вход, и получить
#     его внутри репозитория нельзя. Пока источника нет, `--enforce` дал бы
#     NORM_ARTIFACT_MISSING на КАЖДОМ прогоне каждой полосы (severity_for в
#     ci_runtime_probe.py привязывает обязательность корпуса именно к enforce).
#
#     Отдельно: группа baseline «тесты норм» к корпусу отношения НЕ имела.
#     Прежняя запись здесь утверждала, что 33 из 35 записей существуют только
#     из-за отсутствующего корпуса и исчезнут после provisioning. Волна 0.0.03
#     это проверила и опровергла: узлы падали на setup фикстуры с
#     AttributeError, потому что тест был написан под запрещённый legacy
#     fallback-провайдер. Тесты переписаны под фактический контракт
#     status_index-only, записи сняты по зелёному прямому прогону, и появление
#     корпуса на них ничего не меняло. Разбор — в шапке
#     scripts/ci_known_failures.txt.
#
# Что уже сделано (2026-09-02):
#   ✔ снят `continue-on-error` у регресс-гейта — новая регрессия красит сборку;
#   ✔ baseline сокращён с 35 записей до 2 починкой тестов, а не перезаписью.
#
# Что осталось, и при каких условиях:
#   1. Блокировка переедет на полосы, когда baseline опустеет: две оставшиеся
#      записи принадлежат WEB и ENG (сроки — в шапке ci_known_failures.txt).
#      Раньше этого блокировка по полосе даёт красный CI на каждом прогоне —
#      измерено в чистой комнате. Отдельного условия по `network` больше нет:
#      прогон под профилем раннера выполнен (см. шапку файла).
#   2. Профиль `--enforce` у Lane и Chaos включается, когда владелец назовёт
#      источник norm-корпуса: задать variables QR_NORM_ARTIFACT_SOURCE_ID /
#      _URL / _VERSION / _SHA256 и, для закрытого источника, secret
#      QR_NORM_ARTIFACT_TOKEN. Правка workflow для подключения НЕ нужна: шаг
#      provisioning есть теперь во ВСЕХ трёх job'ах, где выполняется probe.
#      Форма приёмки — docs/ops/NORM_ARTIFACT_SOURCE.md.
#   3. После появления корпуса — по JUnit подтвердить судьбу каждой записи
#      baseline поимённо и пересобрать его ОДИН раз в том же окружении
#      (`ci_regression_gate.py --record`; он сам откажет без корпуса).
#
# Job `chaos` остаётся неблокирующим независимо от этого: его назначение —
# видеть недетерминизм.
