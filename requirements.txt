# Runtime центра.
#
# Здесь — ГРАНИЦЫ совместимости (знание о том, что заведомо ломается), а не
# версии. Ровно проверенный tuple задаёт constraints-qr-v1.txt из §4.1
# quality/runtime contract v1; воспроизводимая установка выглядит так:
#   python -m pip install -c constraints-qr-v1.txt -r requirements.txt
# Без `-c` файл остаётся честным описанием допустимого диапазона, но
# воспроизводимости не даёт: резолвер выберет то, что лежит на PyPI сегодня.

# Верхняя граница обязательна. Открытый диапазон уже пропустил 0.141.1, где
# include_router кладёт в app.routes объект-обёртку вместо самих маршрутов:
# проверки «ручки нет» начали проходить вакуумно, и слепым оказался в том числе
# kill-switch воркерного API. Повышать границу — отдельной задачей с прогоном
# интроспекции маршрутов (tests/test_distributed_workers_flag_off.py).
#
# Уточнение W0-INT-01 (2026-09-01) — чтобы следующий читатель не прочитал абзац
# выше как «0.141.1 запрещена»: граница `<0.142` эту версию ДОПУСКАЕТ и всегда
# допускала, закрыты 0.142 и выше. Слепым был плоский обход `app.routes` в
# самих тестах, а не маршрутизация: тем же коммитом 2a60c367 обход научили
# спускаться в `original_router`, и живой утечки периметра тогда найдено не
# было. Перепроверено на 0.141.1 из constraints-qr-v1.txt:
# tests/test_distributed_workers_flag_off.py — 16 passed, включая негативный
# контроль kill-switch (флаг off → 0 ручек /api/v1/worker, флаг on → 20).
# Поэтому frozen receipt §4.1 (fastapi 0.141.1) и эта граница НЕ противоречат
# друг другу: рецепт пинит версию внутри разрешённого диапазона.
fastapi>=0.115.0,<0.142
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0
aiofiles>=23.0
pydantic>=2.5
openai>=1.60.0
python-dotenv>=1.0.0
google-genai>=1.0.0
psutil>=5.9.0
requests>=2.31.0
opencv-python-headless>=4.8.0

# ── добавлено W0-INT-01: пакеты, которые центр использует, но не объявлял ────
# Это не расширение поверхности, а закрытие manifest gap из §4.1 контракта
# («clean install только из requirements.txt + requirements-proto.txt неполон
# как минимум на passlib, pytest-asyncio, PyMuPDF, Pillow, openpyxl…»). До сих
# пор они приезжали транзитивно из requirements-worker-pipeline.txt — то есть
# центр стоял на манифесте СТОРОННЕГО VPS, и именно оттуда в clean-room
# приходили чужие пины fastapi/pydantic (находка CR-2). Заметить пропажу
# трудно: три из четырёх импортируются лениво, внутри обработчика, поэтому
# приложение стартует и падает только на первом реальном обращении — а
# `pip check` такие пропуски не видит вовсе (§4.1).
passlib>=1.7.4         # backend/app/core/portal_auth.py: pbkdf2_sha256; импорт на уровне модуля — без него не поднимается даже backend.app.main (проверено блокировкой импорта)
openpyxl>=3.1.5        # ленивый импорт в routers/external_register.py:202 и генерации Excel-отчёта; сейчас пропажа маскируется под HTTP 500 «openpyxl not installed»
PyMuPDF>=1.27.2        # fitz: ленивый импорт в crop_blocks и во всех модулях block_grounding
pillow>=12.2.0         # PIL: ленивый импорт в llm_runner, local_vision_provider, отчётах по отклонённым находкам
