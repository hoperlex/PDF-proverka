# G0-readiness delta

**Дата:** 2026-09-02<br>
**Выпустил:** интегратор `W0-INT-01`<br>
**Основание:** `WAVE_0_0_03_PLAN.md` §5.7 требует выпустить G0-readiness delta
отдельно от расписки задачи и без заявления о прохождении общего Gate G0.<br>
**Источник условий:** [`HYBRID_REWRITE_ROADMAP.md`](HYBRID_REWRITE_ROADMAP.md) §6,
раздел «Gate G0».

## Зачем этот документ

Закрытие `W0-INT-01` в программе иногда читают как «почти G0». Это неверно, и
дешевле всего опровергнуть это списком, а не рассуждением. Ниже каждое условие
G0 с проверяемым состоянием на текущий срез.

Обозначения:

- **выполнено** — проверено интегратором, указано чем;
- **не выполнено** — проверено, отсутствует;
- **вне компетенции интегратора** — условие принадлежит владельцу другой
  задачи, и заявлять по нему «выполнено» интегратор не вправе. Это НЕ значит
  «вероятно выполнено».

## Состояние по условиям

| # | Условие G0 | Состояние | Чем проверено |
| --- | --- | --- | --- |
| 1 | явные записи `W0-DEC-01` и `W0-DEC-02`; Bible и ADR-0002 не считаются принятыми по факту старта кода | вне компетенции интегратора | задачи упоминаются в документах программы; наличие самих РЕШЕНИЙ подтверждает владелец программы |
| 2 | приняты ADR-0006–0013, ADR-0017 и ADR-0018 | **не выполнено** | `docs/architecture/adr/`: файлов ADR-0007…ADR-0012 не существует вовсе; ADR-0013 и ADR-0017 в реестре имеют статус `proposed`; приняты только ADR-0006 и ADR-0018 |
| 3 | для ADR-0014 принята числовая retention matrix либо каждый незакрытый класс имеет owner/deadline | **не выполнено** | ADR-0014 в реестре `proposed` |
| 4 | определены источники истины и владельцы contracts | частично | ADR-0018 принят и даёт domain contract v1; полноту по остальным contracts подтверждает владелец contracts |
| 5 | 20–30 критических journeys имеют deterministic fixtures, хотя бы один LLM-journey проходит replay | вне компетенции интегратора | владелец QA/journeys |
| 6 | inventory не содержит неразобранной категории данных | вне компетенции интегратора | владелец inventory |
| 7 | baseline содержит P50/P95/MAX, error rate, RSS, disk и стоимость | вне компетенции интегратора | владелец performance baseline; это НЕ тот baseline, что `scripts/ci_known_failures.txt` |
| 8 | `W0-SEC-03` закрыла EXC-0001 либо G0 не пройден | **не выполнено** | [`EXCEPTIONS.md`](EXCEPTIONS.md): EXC-0001 (`PORTAL_AUTH_ENABLED` default `false`) имеет статус `active` |
| 9 | redaction contract действует, durable audit отделён от diagnostic logs (`W0-LOG-01`) | вне компетенции интегратора | владелец `W0-LOG-01` |
| 10 | матрица `W0-STD-01` закрыта либо помечена истёкшим decision timeout | вне компетенции интегратора | в реестре стандартов запись о неподтверждённой актуальности корпоративного стандарта v3.1 сохраняется |
| 11 | target layout даёт непересекающиеся ownership zones | выполнено | ADR-0006 принят 2026-09-01 и зафиксирован как frozen input |
| 12 | `W0-INT-01` закрыта: clean-room regression с пригодным JUnit, probe зелёный, frontend gate без необъяснённых падений, baseline не вечный allowlist | **не выполнено** — но существенно ближе | см. раздел ниже |
| 13 | legacy sustainment register соблюдает предел 1 active capability slot; human integration hours имеют denominator и 20% reporting target | вне компетенции интегратора | владелец программы |
| 14 | неизвестные/неоднозначные данные имеют статус, владельца и решение | вне компетенции интегратора | владелец inventory |

## Условие 12 подробно: что в `W0-INT-01` сделано и что осталось

Сделано и проверено:

- clean-room прогон пяти полос на non-root профиле даёт пригодный JUnit и
  расписку §8 с полной provenance у каждой полосы;
- capability probe зелёный во всех пяти полосах в режиме `--ci`;
- frontend gate (`test`/`lint`/`typecheck`/`build`) зелёный, Node пинится явно;
- baseline перестал быть вечным allowlist: из 35 записей осталось 2, и 33 сняты
  не перезаписью, а починкой тестов по зелёному прямому прогону;
- регресс-гейт и полосы `unit`/`contract`/`integration` БЛОКИРУЮТ (маскировка
  кода 1 и `continue-on-error` сняты 2026-09-02);
- первичка всех прогонов закреплена манифестами с SHA-256.

Не сделано:

- **norm corpus не подключён.** §3.3 контракта прямо говорит, что прогон без
  выбранного корпуса не считается G0 receipt. Источник принадлежит владельцу
  программы, форма приёмки — [`../ops/NORM_ARTIFACT_SOURCE.md`](../ops/NORM_ARTIFACT_SOURCE.md);
- профиль полос остаётся `--ci`, а не `--enforce` — прямое следствие
  предыдущего пункта;
- полоса `network` не блокирует: пять её недетерминированных узлов закрыты, но
  полоса ЦЕЛИКОМ под нагрузочным профилем раннера не прогонялась;
- ни один прогон не снят на образе `ubuntu-24.04` — вся evidence с
  восьмиядерной машины разработчика.

## Вывод

Gate G0 **не пройден**, и `W0-INT-01` — не главное, чего для него не хватает.
Шесть обязательных ADR не написаны, два приняты не полностью, активно
security-исключение EXC-0001, а половина условий принадлежит владельцам других
задач и интегратором не проверяется в принципе.

Массовая генерация W1-кода, новый `pyproject.toml` и skeleton
`src/auditmanager/**` не начинаются: roadmap §6 говорит об этом прямо.
