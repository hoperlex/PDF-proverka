# Каталог режимов отказа

Сводка всех подтверждённых измерением режимов отказа текущего представления. Каждая строка:
что ломается → чем это измерено → чем чинится в v0.2.

Источники: `orchestrator_findings.md` (O1–O13) и `*_FINDINGS.md` каждого зонда.

---

## A. Дефекты извлечения (ломают данные ещё до сравнения)

| # | Режим | Измерение | Чинится |
|---|---|---|---|
| A1 | **Повёрнутая страница читается в другой системе координат.** `block_rect` строится из `page.rect` (повёрнутая), а `get_drawings()`/`get_text()` возвращают неповёрнутые координаты; `get_pixmap()` — повёрнутые. Описание и картинка относятся к разным частям листа. | Подтверждено **четырьмя независимыми зондами**. Пересечение областей: `vk_node_plan` **0.00 %** (0 из 217 094 точек), `vk_plan` 0.2–9.8 %, `vk_nodes` 22–32 %, `eom` слева 58–63 %. 7–8 из 20 блоков затронуты. В корпусе повёрнуто **4–6 % страниц**, но **27.5 % документов** содержат хотя бы одну. | явная фиксация системы координат + `derotation_matrix` (production уже так делает в 11 модулях) |
| A2 | **Анизотропная нормировка.** `x/w, y/h` с разными делителями искажает каждый угол и каждую форму, если пропорции кропов различаются. | Анизотропия по 10 парам **0.927–1.137**. При анизотропии 1.137 отрезок, лежащий под 45° на одной стороне, ложится под 41.3° на другой — расхождение 3.7°. Допуск по углу в компараторе — `max(1.0, tol·500)`; только грубый 1 % это скрывает. | изотропная нормировка, аспект — отдельный факт |
| A3 | **Кап «по самым длинным».** Отбираются самые длинные сегменты — то есть рамка, оси и длинные трассы, которые заведомо не меняются. | `ss_plan_dense`: компаратор видит **14.2 %** блока; покрытие оставленного **1.000**, отброшенного **0.763**. `vk_nodes`: видно **8.5 %**. Кап выбрасывает **50.7–62.5 %** примитивов целиком и **39.7 %** пускает в граф изувеченными. | стратифицированный отбор (по регионам/объектам/толщине), а не по длине |
| A4 | **Обрезка текста границей блока.** `get_text(clip=…)` рвёт span по границе. | `4х(1х120)+1х70` → `20)+1х70`; `+4,450` → `+4`; `Количество` → `чество`. **130 из 6650** span'ов в **24 из 42** блоков; худший блок **28 из 37 = 76 %**. | брать текст без клипа и фильтровать по bbox |
| A5 | **Неполный `/ToUnicode` в CAD-подшрифте.** Кириллица не отображена → управляющие символы; блочный гейт объявляет весь текстовый слой недекодируемым. | 41 диапазон покрывает только латиницу, цифры и пунктуацию. При этом **214 из 421** span'ов `vk_nodes` читаются идеально и несут голые размерные значения (189 из 214 содержат цифру) — и все выбрасываются вместе со сломанными. Деление не «цифры целы, буквы сломаны»: 141 из 207 сломанных span'ов тоже содержат цифру, потому что составные записи ломаются на знаке умножения и кириллице (`Ø16×1/2"`, `×3,5`). | per-span readable + **хеш контура глифа** как identity: 99/99 кодов совпали, F1 сломанной части **0.000 → 0.456/0.502/0.588** |
| A6 | **Гранулярность штрихов — свойство экспортёра, и она меняется между версиями.** | ЭОМ: слева **75.3 %** сегментов короче 0.001 блока, справа **26.9 %**. Медиана длины штриха отличается в **4.5 раза** (0.00048 против 0.00215). Любой радиус кластеризации, взятый от собственной медианы стороны, строит две стороны в разных метриках. | масштабы считать **совместно для пары** или брать масштабо-свободные величины |
| A7 | **CAD-текст, обведённый контурами, невидим полностью.** | 14 страниц корпуса: 0 шрифтов, 0 текста, без растровой подложки. На ГП `search_for("Тротуар") == []` при полностью читаемом рендере. Fact recall такой пары — **0.00**. Страница 5 ЭМ2-ПА: 7124 пути, 146 189 элементов, 92 135 операций Безье. | детектор глиф-слов (кластеризация мелких заливок в слова) + OCR только этих слов; **или** честный `TEXT_ABSENT` и маршрут в Vision |
| A8 | **Растровый блок скрывает изменение и объявляется идентичным.** | График вентилятора ОВ: модель `…-5,5-2 → …-9-3-2-V1`, рабочая точка `Pv 795→757 Па`, `Ps 633→692 Па`. В кропе 2 векторных примитива, 0 текста → `exact_vector_signature_equal = true`, вердикт **IDENTICAL**. Попиксельная разница — 2.56 %. | обязательный гейт «в блоке есть растр» → маршрут в Vision; **никогда** не выдавать IDENTICAL при нулевой векторной опоре |
| A10 | **«Невидимая краска»: белые заливки без обводки участвуют в геометрии, но ничего не рисуют.** | `vk_plan` справа: **31 839** невидимых путей = **44 %** сегментов; `ss_scheme` справа: 120 белых прямоугольников-выбивок под подписями. Отбрасывание невидимой краски убирает **94–100 %** кажущегося расхождения: относительная разница числа сегментов 0.4099 → **0.0000** (`ss_scheme`), 0.3188 → 0.0195 (`vk_plan`). Track A при этом сообщает по `ss_scheme` geometry similarity 0.8664. | фильтр: заливка (1,1,1), без обводки, на белой подложке — не рисует ничего |
| A9 | **Перепаковка растра между версиями.** | **1099 из 3096** сопоставленных пар страниц меняют число изображений; ОВ: **274 плитки → 1 изображение** при идентичном содержимом. | сравнение растра по содержимому, а не по числу объектов |

## B. Дефекты представления (данные верны, но сказать нечего)

| # | Режим | Измерение | Чинится |
|---|---|---|---|
| B1 | **Нет объекта — нечего называть.** Вывод возможен только как счётчик анонимных примитивов. | Из 124 строк «изменений» **93** — сопоставление текста по позиции, остальные — счётчики. Из 326 `value_changes` **4.6 %** — «число → число», **56.4 %** — управляющие символы. Примеры: `Согласовано → этажного`, `ГАП → Принципиальная`. | слой объектов |
| B0 | **Объект устойчивее сегмента и топологии — но у наивной кластеризации нет масштаба объекта.** | На 9 парах без изменения чертежа точно совпало: число кандидатов-символов **8 из 9**, число сегментов 4 из 9, число компонент 2 из 9. Объектный diff даёт точный ноль на **5 парах из 10**. Но радиусная кластеризация «сама по себе» не работает: 486 → 429 → 398 → **3** → 1 за один шаг радиуса. | три правила поверх близости: фильтр невидимой краски, склейка штриховых цепочек, потолок масштаба символа (92 фантомных кластера → 25 при 20 реальных приборах) |
| B2 | **`repeated_elements` фингерпринтит PDF-пути, а не символы.** | На листе 15 камер + 5 ОСПД; `repeated_elements` даёт **одну группу из 15**, потому что 4 символа ОСПД лежат внутри одного примитива. Топ-мотивы корпуса — `filled_polygon` из 4 сегментов и `circle` ровно из 24 (константа ресэмплинга). | мотив = связная компонента сырых сегментов после снятия длинных «сетевых» отрезков: камеры **15/15**, ОСПД **5/5**, ⊠ **14/14** |
| B3 | **Кардинальность по геометрии ненадёжна.** | Точность утверждения «2 → 4»: `repeated_elements` Jaccard **0.000**; граф отношений **≈31 %**; толерантные мотивы **26 %** (6 верных из 23). Ложные срабатывания на «тихих» парах: 169 / 9 из 13 / 5 из 8. | **кардинальность по обозначениям**: `Wh 0→4, QD 0→4, QF 0→4, ЩМкв 0→4`, **0** ложных на 7 тихих парах |
| B4 | **Форма не инвариантна к кодированию PDF.** | Один и тот же ряд кружков шин: левый PDF — 34 примитива `circle`, правый — 147 `curve`. Отсюда ложное «16 круглых элементов исчезли». CAD-округление 0.24 pt (2.8 %) расщепляет 14 одинаковых символов на 8 + 5 + 1. | дескриптор, нечувствительный к разбиению: тот же маркер из 30 сегментов в v001 и 11 в v002 даёт межверсионное расстояние 0.0945 при расстоянии до чужого объекта 1.8891 — разделение **16.7×**. Хеш формы не переносится: из ~50 классов на обеих сторонах есть 4–8 |
| B5 | **`anchors` не несёт информации.** | На 374 размерных span'ах, где правильность известна объективно, confidence = `high` у **374 из 374**, точность привязки **0.348**. В плотных блоках 20.9–185.8 равно подходящих примитивов в радиусе. | уверенность = **число конкурентов в коридоре**: ≤2 → 0.677, ≥3 → 0.024 |
| B6 | **Единица текста выбрана неверно.** | 6751 span → 4597 печатных строк (−31.9 %); **23.5 %** многоспановых строк содержат span'ы, не согласные между собой о собственном референте. На `ss_simple_node` — 0 из 3. | печатная строка; для переносов — абзац |
| B7 | **Топологические счётчики не являются свойствами чертежа.** | `connected_components` двигается в **282 раза**, `endpoints` в **259 раз** от одного допуска. Self-similarity блока с самим собой **0.43–0.77** при гейте 0.85 — **10 из 10** блоков проваливают собственный гейт. | снять с роли гейта; T-соединения оставить как гипотезы для сборки объектов |
| B8 | **`x_crossings_unconnected` измеряет не то, что называет.** | **90–99.8 %** пересечений соединяют сегменты уже одной компоненты; **35–62 %** были склеены T-проходом на том же месте. Из 15 просмотренных глазом — 8 чистый фон (включая **подпись от руки в штампе**), инженерный смысл максимум у 4. При усечении на 5000 с обеих сторон компаратор видит `5000 vs 5000` и ставит **1.0**. | удалить как счётчик |
| B9 | **Символ от прямоугольника не отделяется ничем геометрическим.** | На 56 размеченных группах: штриховка от символа отделяется (recall 0.93 / precision 0.87), а символ от простого прямоугольника — лучшее 0.83 при recall 0.36. | признак «внутри есть текст» + принадлежность семейству + контекст отношений |
| B10 | **Легенда недостижима отношениями близости.** | Самый стереотипный текст-символьный макет: надёжно привязано **3 из 46 = 6.5 %**, 12 из 46 остаются несвязанными даже с полосовым отношением. | отдельный табличный/полосовой примитив для легенды |
| B11 | **Размер привязывается к ближайшей линии, а не собирается как объект.** | Сырая проверка «конец сегмента рядом с линией» подделывает границу из плеча засечки соседней цепочки (истинный интервал 55.56 pt разрезан на 31.38 + 24). Сборка коллинеарных плеч в одну засечку: доля привязок с ошибкой ≤1 % 57 → 58, медиана невязки вдвое меньше. Требование выносной ножки: подтверждённых привязок 54 → 87 (+61 %). Из 137 неподтверждённых 44 (32 %) — это выноски, а не размеры. | размер как объект: выносные + размерная линия + два собранных терминатора + число + сверка с масштабом листа |

## C. Дефекты решения (пороги и статусы)

| # | Режим | Измерение | Чинится |
|---|---|---|---|
| C1 | **Порог по тексту нельзя настроить — требования противоречат друг другу.** | Реальное изменение площадей `17,68→17,37`, итог `497,57→497,24` → similarity **0.995** при пороге 0.92 → `NEAR_IDENTICAL`. Чистая перевёрстка таблицы при байт-идентичном тексте → geometry 0.841 → `STRUCTURE_CHANGED`. | сравнивать значение **внутри своей ячейки/объекта**, а не в мультимножестве блока |
| C2 | **Единственный верный вердикт «значения изменились» держится на счётчиках PDF-операторов.** | Обнуление `extraction.source_item_counts` (`l` и `re`) превращает верный `ss_scheme_text_changed` в неверный `STRUCTURE_CHANGED`. Собственные similarity пары (0.866 / 0.733) ниже порогов альтернативной ветки. | вердикт из evidence-леджера, а не из эвристики упаковки |
| C3 | **Лестница допусков компенсирует сдвиг кропа, а не шум.** | `vk_nodes`: покрытие 0.095 → 0.365 → 0.713 → 0.991 на 0.10/0.25/0.50/1.00 %. После 4-параметрической поправки при **0.25 %** — **0.913**; `ss_plan_dense` — **0.9997**. | выравнивание как отдельный шаг; допуск **зарабатывается** |
| C4 | **Сдвиг кропа 0.5 % ломает всё.** | Блок против самого себя при сдвиге 0.5 %: `IDENTICAL` теряется у **3 из 4** самопар и выдумываются изменения значений (`"Sto → "Stop`). Один из двух верных вердиктов о реальном изменении разрушается. Сдвиг **6 %** на той же странице даёт `STRUCTURE_CHANGED` (geometry 0.475). | выравнивание + атрибуция приграничных различий как артефактов кропа |
| C5 | **Сигнал ниже шума на плотных блоках и выше крыши на разреженных.** | Инъекция 20 ответвлений и 20 кружков в плотный план меняет topology similarity на **0.0136** — в 34.7 раза меньше артефакта допуска; оба гейта проходят → `NEAR_IDENTICAL`. **Одно** ответвление в простом узле → `STRUCTURE_CHANGED`. | пороги, нормированные на плотность; сравнение по объектам, а не по долям сегментов |
| C6 | **При капе добавление геометрии уменьшает счётчики.** | Инъекция 20 ответвлений в `ss_plan_dense`: `segments_used` остаётся ровно 8000, `branch_points` **4636 → 4632**, то есть вниз. | стратифицированный отбор + счётчики только по объектам |
| C7 | **Система печатает пользователю то, чему сама не доверяет.** | У всех трёх ВК-пар `reliable=false`, текст исключён из решения — и при этом каждая печатает по 93–100 строк `Текст/значение` по этому же тексту. | evidence-леджер с provenance: недоверенное не попадает в вывод |
| C8 | **Сдвиг нумерации страниц — норма, а не край.** | **1620 из 3096** сопоставленных страниц (52.3 %) меняют индекс; 896 страниц только слева, 725 только справа. | сопоставление листов по содержанию, 1↔1 недостаточно |

## D. Что НЕ подтвердилось (фальсифицированные гипотезы)

Фиксируется отдельно, чтобы не тратить на это работу.

| Гипотеза | Что измерено |
|---|---|
| «сглаживание кривых портит топологию» | максимальная ошибка хорды **0.067 pt = 0.00012** нормализованных единиц; 0 % выше допуска. Оба маршрута рендера окружности дают покрытие **1.0** друг против друга на всех допусках. Риск существует только если экспортёры используют **разное** число вершин дуги — в этом корпусе у всех near-circular форм ровно 24 вершины |
| «лимит 1000 контуров в `nested_contours` срабатывает» | не срабатывает: максимум 382 замкнутых контура. Проблема `nested_contours` в другом: на `ss_plan_dense` **все 381** вложений имеют **один** контейнер — рамку листа, и **50.4 %** из них ложны при проверке по реальному полигону |
| «кап смещён к рамке и фону» | частично неверно: на `ss_plan_dense` оставленное действительно более осевое (0.789 против 0.491), но на `ar_wall_sections` — **менее** (0.249 против 0.540). Пространственное покрытие почти не меняется (85.6–100 % занятых ячеек). Кап разрушает **объекты**, а не смещает к рамке |
| «локальная топология мотива улучшит фингерпринт» | S2 (профиль степеней) даёт **−1 %** групп и расщепляет настоящую группу из 24 кружков на 12 + 11 |
| «соседние отношения улучшат фингерпринт» | S4 разрушителен внутри хеша идентичности: группа из 56 маркировочных кружков рассыпается на 30 / 18 / 7 |
| «допуск квантования мотива можно подобрать» | эффект немонотонен: наибольшая группа `ar_plan` при q = 0.02…0.2 даёт **34 / 53 / 56 / 56 / 56 / 42** |

## E. Приоритет: что бьёт больнее всего

Сортировка по произведению «частота × ущерб», по измеренным числам.

1. **A1 — система координат.** 27.5 % документов, и данные относятся к другой части листа.
   Всё остальное поверх этого недействительно.
2. **C1/C4 — нестабильность к кропу.** П→РД всегда меняет кадрирование. 0.5 % ломает вердикт.
3. **B1/B3 — нет объекта и нет надёжной кардинальности.** Без этого нельзя написать ни одной
   фразы, которую ждёт эксперт.
4. **A3/C5/C6 — капы.** На плотных листах система смотрит на 8–14 % чертежа и не видит
   инъекцию 40 элементов.
5. **A5/A4/B6 — текст.** 36 % значений выпадает, 1.9 % span'ов обрезано, 49 % текста ВК
   объявлено недекодируемым при существующем детерминированном лечении.
6. **A7/A8 — обведённый текст и растр.** Редко, но даёт **молчаливое** «IDENTICAL» на реальном
   изменении — худший из возможных отказов.
