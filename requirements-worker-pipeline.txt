# Зависимости КОНВЕЙЕРА на воркере (этап реального VPS-пилота).
#
# Почему отдельный файл, а не дополнение requirements-worker.txt.
# requirements-worker.txt описывает САМ АГЕНТ (httpx + psutil) — он ставится
# даже туда, где реального аудита не будет никогда, и остаётся честно
# коротким. Но с момента, когда воркер начал выполнять `audit_pipeline_v1`,
# дочерний процесс `backend.app.pipeline.remote_audit_runner` запускается ТЕМ
# ЖЕ интерпретатором (если не задан AUDIT_WORKER_PIPELINE_PYTHON) и падает на
# импорте pydantic/fastapi/dotenv, а на этапе crop_blocks — на fitz.
#
# Список выведён из фактического импорт-графа кода конвейера, а версии
# ПРИБИТЫ по тому, что стоит на центре (/opt/py312, python 3.12.13): снимок
# моделей и профиля дисциплины едет в пакете задания, а вот версии библиотек
# не едут ничем — расхождение здесь даёт «работает на центре, падает на
# воркере» без единого сообщения о причине.
#
# Установка (после requirements-worker.txt, в тот же venv):
#   <venv>/bin/pip install -r requirements-worker-pipeline.txt

# ВНИМАНИЕ (W0-INT-01, 2026-09-01): этот файл — ОТДЕЛЬНЫЙ профиль развёртывания
# и НЕ входит в тестовое окружение центра. Ставить его вместе с
# constraints-qr-v1.txt нельзя: пины ниже противоречат reference receipt §4.1
# quality/runtime contract v1 (fastapi 0.115.6 против 0.141.1, starlette 0.41.3
# против 1.6.0, pydantic 2.10.4 против 2.13.4, openai 2.31.0 против 3.3.1), и
# pip честно завершится ResolutionImpossible. Это не поломка, а тот самый
# сигнал, которого не хватало: пока манифесты складывали «все шесть подряд»,
# центр в clean-room молча поднимался на 0.115.6, и снапшот HTTP-контракта
# расходился на 27 эндпоинтов (находка CR-2 в
# docs/architecture/receipts/W0-OPS-03-cleanroom-rehearsal.json).
#
# Оговорка «версии прибиты по тому, что стоит на центре» ниже с этого момента
# ОТСТАЛА: центр воспроизводится по constraints-qr-v1.txt. Расхождение оставлено
# намеренно — перевод конвейера на qr-v1 меняет продакшен-профиль стороннего VPS
# и требует прогона на самом VPS, а не решения на уровне манифеста.

# ── ядро конфигурации и валидации ────────────────────────────────────────────
fastapi==0.115.6
starlette==0.41.3
pydantic==2.10.4
pydantic-core==2.27.2
python-dotenv==1.0.1

# ── графика и документы ──────────────────────────────────────────────────────
# PyMuPDF (fitz) — рендер кропов блоков в crop_blocks; без него этап падает.
PyMuPDF==1.27.2.2
pillow==12.2.0
openpyxl==3.1.5
numpy==2.4.4

# ── сеть и провайдеры ────────────────────────────────────────────────────────
# requests/openai нужны импортам конвейера. На этом этапе ключей нет, а
# PAID_API_ENABLED=false и подделки CLI перекрывают путь к реальным моделям —
# библиотеки присутствуют, но вызовам некуда идти.
requests==2.31.0
openai==2.31.0

# ── авторизация портала ──────────────────────────────────────────────────────
# backend.app.core.portal_auth импортируется по цепочке от backend.app.core.config.
passlib==1.7.4
